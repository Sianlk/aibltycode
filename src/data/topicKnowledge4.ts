// Autonomic Mastery Knowledge Base — Part 4 (Java: operators, strings, control flow,
// arrays, collections, OOP, exceptions, generics, streams)
import type { TopicRecord } from "./topicKnowledge";

export const javaTopics4: Record<string, TopicRecord> = {
  "type-casting": ["Type Casting", "Changing a value from one type to another — automatically when it is safe, manually when it might lose data.", "Pouring a small cup into a bucket is safe; pouring a bucket into a cup needs care and spills.", "W.N = Widening is automatic, Narrowing needs a cast", "int small = 10;\ndouble big = small;        // widening, automatic\ndouble price = 9.99;\nint rounded = (int) price;  // narrowing, manual -> 9", "What happens to 9.99 when you cast it to int?", "The decimal part is cut off, leaving 9", "It rounds up to 10", "The program crashes"],
  "constants": ["Constants & final", "A value marked final can be set once and never changed, protecting important values from accidental edits.", "A price carved into stone outside a shop — nobody can rub it out.", "F.U.O = Final, UPPER_CASE, Once only", "public static final double VAT_RATE = 0.20;\nfinal int MAX_PLAYERS = 4;\n// MAX_PLAYERS = 5; // compile error", "What happens if you reassign a final variable?", "The code will not compile", "It changes silently", "It resets to zero"],
  "variable-scope": ["Variable Scope", "The region of code where a variable exists — local inside a method, instance inside an object, static across the whole class.", "A tool in your pocket (local), in the house toolbox (instance), in the shared community shed (static).", "L.I.S = Local, Instance, Static", "public class Game {\n  static int totalGames;   // class-wide\n  int score;               // per object\n  void play() {\n    int bonus = 5;         // local only\n    score += bonus;\n  }\n}", "Where can a local variable be used?", "Only inside the block where it is declared", "Anywhere in the program", "Only in the main method"],
  "comparison-ops": ["Comparison Operators", "Symbols that compare two values and produce true or false, powering every decision your program makes.", "A set of scales: it only ever tips one way or the other.", "E.N.L.G = Equal, Not-equal, Less, Greater", "int a = 5, b = 8;\nSystem.out.println(a == b);  // false\nSystem.out.println(a != b);  // true\nSystem.out.println(a < b);   // true\nSystem.out.println(a >= 5);  // true", "What is the difference between = and ==?", "= assigns a value, == compares two values", "They mean the same thing", "== assigns and = compares"],
  "logical-ops": ["Logical Operators", "Combine true/false conditions: AND needs both, OR needs one, NOT flips the answer.", "A club door: over 18 AND on the list gets you in.", "A.O.N = And &&, Or ||, Not !", "boolean member = true, paid = false;\nif (member && paid) System.out.println(\"Enter\");\nif (member || paid) System.out.println(\"Maybe\");\nif (!paid) System.out.println(\"Please pay\");", "When is a && b true?", "Only when both a and b are true", "When either one is true", "When both are false"],
  "assignment-ops": ["Assignment Operators", "Shortcuts that update a variable using its own current value.", "Topping up a mug rather than pouring a fresh one.", "P.M.T.D = Plus, Minus, Times, Divide equals", "int total = 10;\ntotal += 5;  // 15\ntotal -= 3;  // 12\ntotal *= 2;  // 24\ntotal /= 4;  // 6", "What does total += 5 mean?", "total = total + 5", "total equals exactly 5", "Compare total with 5"],
  "bitwise-ops": ["Bitwise Operators", "Operators that work on the individual 1s and 0s inside a number — used for flags, graphics and speed.", "Flicking individual light switches on a row rather than changing the whole room.", "A.O.X.N.S = And, Or, Xor, Not, Shift", "int a = 6;   // 0110\nint b = 3;   // 0011\nSystem.out.println(a & b); // 2  (0010)\nSystem.out.println(a | b); // 7  (0111)\nSystem.out.println(a ^ b); // 5  (0101)\nSystem.out.println(a << 1);// 12 (1100)", "What does << 1 do to a number?", "Doubles it by shifting bits left", "Halves it", "Reverses its digits"],
  "ternary-op": ["Ternary Operator", "A one-line if/else that produces a value: condition ? valueIfTrue : valueIfFalse.", "A fork in a path with a signpost giving you the answer immediately.", "C.Q.C = Condition ? then : else", "int age = 20;\nString type = age >= 18 ? \"Adult\" : \"Child\";\nint max = a > b ? a : b;", "What does age >= 18 ? \"Adult\" : \"Child\" return when age is 12?", "\"Child\"", "\"Adult\"", "true"],
  "string-methods": ["String Methods", "Built-in tools on every String for measuring, slicing, searching and changing text.", "A Swiss army knife attached to every piece of text you own.", "L.S.I.R.T = length, substring, indexOf, replace, trim", "String s = \"  Java Rocks  \";\nSystem.out.println(s.trim());\nSystem.out.println(s.indexOf(\"Rocks\"));\nSystem.out.println(s.replace(\"Rocks\", \"Rules\"));\nSystem.out.println(s.toLowerCase());", "What does substring(0, 4) return from \"Java Rocks\"?", "\"Java\" — characters 0 up to but not including 4", "\"Rocks\"", "\"J\""],
  "string-comparison": ["String Comparison", "Use equals() to compare the text inside two Strings; == only asks whether they are the same object in memory.", "Two identical letters in different envelopes: same content, different envelope.", "E.C.I = Equals for content, == compares Identity", "String a = new String(\"hi\");\nString b = new String(\"hi\");\nSystem.out.println(a == b);            // false\nSystem.out.println(a.equals(b));       // true\nSystem.out.println(a.equalsIgnoreCase(\"HI\")); // true", "Why can a == b be false for two identical Strings?", "== compares memory references, not the text", "Strings can never be equal", "== only works on numbers"],
  "string-builder": ["StringBuilder", "A changeable text buffer that is far faster than joining Strings repeatedly in a loop.", "Writing on a whiteboard instead of printing a new page each time you add a word.", "A.I.D.R = append, insert, delete, reverse", "StringBuilder sb = new StringBuilder();\nfor (int i = 0; i < 5; i++) sb.append(i).append(\",\");\nsb.reverse();\nSystem.out.println(sb.toString());", "Why prefer StringBuilder inside a loop?", "Strings are immutable, so each join makes a new object — StringBuilder edits in place", "It looks nicer", "Strings do not work in loops"],
  "string-formatting": ["String Formatting", "Building neat text with placeholders for numbers, decimals and alignment instead of messy joining.", "A form with blank boxes you slot values into.", "S.D.F = %s String, %d digit, %.2f decimals", "System.out.printf(\"%s scored %d (%.1f%%)%n\", \"Ana\", 42, 87.5);\nString row = String.format(\"%-10s|%5d\", \"Item\", 7);", "What does %.2f do?", "Prints a decimal number to 2 decimal places", "Prints two numbers", "Prints a percentage sign"],
  "nested-if": ["Nested If Statements", "An if inside another if, so a second question is only asked when the first answer is true.", "Airport security: first check you have a ticket, then check your bag.", "O.I.T = Outer check, Inner check, Then act", "if (loggedIn) {\n  if (isAdmin) System.out.println(\"Admin panel\");\n  else System.out.println(\"User area\");\n} else {\n  System.out.println(\"Please log in\");\n}", "When does the inner if run?", "Only when the outer condition is true", "Always", "Only when the outer condition is false"],
  "switch-expressions": ["Switch Expressions", "Modern switch that returns a value using arrow syntax with no fall-through and no break needed.", "A vending machine that hands you the item directly rather than describing it.", "A.N.Y = Arrow, No break, Yields a value", "String kind = switch (day) {\n  case 6, 7 -> \"Weekend\";\n  case 1, 2, 3, 4, 5 -> \"Weekday\";\n  default -> \"Unknown\";\n};", "What is the main advantage of arrow switch?", "It returns a value and cannot fall through", "It runs faster than everything", "It removes the need for variables"],
  "do-while": ["Do-While Loops", "A loop that runs its body first and checks the condition afterwards, so it always runs at least once.", "Tasting soup before deciding whether to add more salt — you always taste once.", "D.B.C = Do body, then Check", "int attempts = 0;\ndo {\n  attempts++;\n  System.out.println(\"Try \" + attempts);\n} while (attempts < 3);", "How many times does a do-while body run at minimum?", "Exactly once, even if the condition is false", "Zero times", "Always three times"],
  "enhanced-for": ["Enhanced For Loop", "A read-through loop that visits every item in an array or collection without index bookkeeping.", "Walking down a buffet, taking each dish in turn without counting plates.", "F.T.I.C = For (Type item : Collection)", "int[] scores = {10, 20, 30};\nfor (int s : scores) System.out.println(s);\nfor (String name : names) System.out.println(name.toUpperCase());", "What is the enhanced for loop best for?", "Reading every element in order without needing indexes", "Removing items while looping", "Counting backwards"],
  "break-continue": ["Break & Continue", "break leaves the loop entirely; continue skips to the next pass.", "break is walking out of the cinema; continue is skipping one boring scene.", "B.E.C.S = Break Exits, Continue Skips", "for (int i = 0; i < 10; i++) {\n  if (i == 5) break;      // stop entirely\n  if (i % 2 == 0) continue; // skip evens\n  System.out.println(i);\n}", "What does continue do?", "Skips the rest of this pass and starts the next one", "Ends the loop", "Restarts the whole program"],
  "nested-loops": ["Nested Loops", "A loop inside a loop — the inner loop finishes fully for each single pass of the outer loop.", "Hands of a clock: the minute hand goes all the way round for every one hour step.", "O.R.I.C = Outer Rows, Inner Columns", "for (int row = 1; row <= 3; row++) {\n  for (int col = 1; col <= 3; col++) {\n    System.out.print(row * col + \"\\t\");\n  }\n  System.out.println();\n}", "If the outer loop runs 3 times and the inner 4, how many inner passes happen?", "12", "7", "4"],
  "parameters-return": ["Parameters & Return", "Parameters carry data into a method; return sends one answer back out.", "A blender: fruit goes in (parameters), smoothie comes out (return).", "I.P.R.O = In Parameters, Return Out", "public static double area(double w, double h) {\n  return w * h;\n}\ndouble a = area(3, 4); // 12.0", "What does the return type tell you?", "The kind of value the method hands back", "How many parameters it takes", "Who can call it"],
  "recursion": ["Recursion", "A method that calls itself on a smaller version of the problem until it hits a stopping case.", "Russian dolls: open one to find a smaller one, until the tiniest solid doll.", "B.S = Base case first, Smaller step next", "public static int factorial(int n) {\n  if (n <= 1) return 1;      // base case\n  return n * factorial(n - 1); // smaller step\n}", "What happens without a base case?", "Infinite calls until a StackOverflowError", "It returns zero", "It runs faster"],
  "varargs": ["Variable Arguments", "A parameter that accepts any number of values, handled inside the method as an array.", "A tray that takes one plate or ten — same tray.", "T.D.N.L = Type... name, Do it Last", "public static int sum(int... nums) {\n  int total = 0;\n  for (int n : nums) total += n;\n  return total;\n}\nsum(1, 2, 3, 4);", "Where must a varargs parameter appear?", "Last in the parameter list", "First", "Anywhere"],
  "array-operations": ["Array Operations", "Walking through an array to read, total, find or change its values.", "Checking every locker in a corridor one door at a time.", "T.S.M.F = Traverse, Sum, Max, Find", "int[] a = {4, 9, 2};\nint sum = 0, max = a[0];\nfor (int i = 0; i < a.length; i++) {\n  sum += a[i];\n  if (a[i] > max) max = a[i];\n}", "What is a[a.length] going to cause?", "ArrayIndexOutOfBoundsException", "It returns the last item", "It returns zero"],
  "multidim-arrays": ["2D Arrays", "An array of arrays, giving you rows and columns like a grid or spreadsheet.", "A chessboard: pick a row, then a column.", "R.C = Row first, Column second", "int[][] grid = new int[3][3];\ngrid[0][0] = 1;\nfor (int r = 0; r < grid.length; r++)\n  for (int c = 0; c < grid[r].length; c++)\n    System.out.print(grid[r][c]);", "What does grid[2][1] mean?", "Row index 2, column index 1", "The 2nd and 1st arrays joined", "Two rows and one column"],
  "array-algorithms": ["Array Algorithms", "Standard recipes for searching (linear, binary) and sorting (bubble, selection) data.", "Finding a name in a phone book: page by page, or splitting it in half repeatedly.", "L.B.S = Linear anywhere, Binary needs Sorted", "// linear search\nfor (int i = 0; i < a.length; i++)\n  if (a[i] == target) return i;\n// binary search needs sorted data\nArrays.sort(a);\nint idx = Arrays.binarySearch(a, target);", "What must be true before binary search works?", "The array must already be sorted", "The array must be small", "The array must contain no duplicates"],
  "arrays-class": ["Arrays Utility Class", "java.util.Arrays gives ready-made tools for sorting, filling, copying, comparing and printing arrays.", "A toolbox that saves you from carving your own hammer.", "S.F.C.T = sort, fill, copyOf, toString", "import java.util.Arrays;\nint[] a = {3, 1, 2};\nArrays.sort(a);\nSystem.out.println(Arrays.toString(a));\nint[] b = Arrays.copyOf(a, 5);", "Why use Arrays.toString(a)?", "Printing an array directly shows a memory reference, not the values", "It sorts the array", "It converts values to text permanently"],
  "arraylist": ["ArrayList Basics", "A resizable list that grows and shrinks as you add and remove items, unlike a fixed array.", "An expanding concertina folder rather than a fixed egg box.", "N.A.G.S = New, add, get, size", "import java.util.ArrayList;\nArrayList<String> names = new ArrayList<>();\nnames.add(\"Ana\");\nnames.add(\"Bo\");\nSystem.out.println(names.get(0));\nSystem.out.println(names.size());", "What is the main advantage over an array?", "It resizes automatically as items are added or removed", "It is always faster", "It can hold any mixture of types safely"],
  "arraylist-methods": ["ArrayList Methods", "The everyday operations: add, get, set, remove, contains, indexOf, clear.", "Editing a shopping list: add, cross off, swap, check.", "A.G.S.R.C = add, get, set, remove, contains", "list.add(\"apple\");\nlist.add(1, \"pear\");\nlist.set(0, \"plum\");\nlist.remove(\"pear\");\nboolean has = list.contains(\"plum\");", "What does remove(1) do on a List<String>?", "Removes the item at index 1", "Removes the value \"1\"", "Removes the first item"],
  "wrapper-classes": ["Wrapper Classes", "Object versions of primitives — Integer, Double, Boolean — needed by collections and offering helper methods.", "A coin inside a presentation case so it can go on the shelf with other objects.", "I.D.B.C = Integer, Double, Boolean, Character", "Integer n = Integer.valueOf(\"42\");\nint parsed = Integer.parseInt(\"7\");\ndouble d = Double.parseDouble(\"3.5\");\nSystem.out.println(Integer.MAX_VALUE);", "Why do collections need wrapper classes?", "Generics only work with objects, not primitives", "Primitives are too slow", "Wrappers use less memory"],
  "autoboxing": ["Autoboxing", "Java automatically converts between primitives and their wrapper objects when needed.", "An automatic gift-wrapping machine at the till — you never have to ask.", "A.U = Auto boxes in, Unboxes out", "List<Integer> nums = new ArrayList<>();\nnums.add(5);          // autoboxing int -> Integer\nint first = nums.get(0); // unboxing Integer -> int", "What risk comes with unboxing?", "A null Integer throws NullPointerException when unboxed", "It always loses precision", "It changes the value"],
  "instance-vars": ["Instance Variables", "Fields that belong to each object, holding that object's own state.", "Every car has its own fuel gauge reading.", "P.O.D = Per Object Data", "public class Player {\n  private String name;   // instance variable\n  private int health = 100;\n  public Player(String name) { this.name = name; }\n}", "How many copies of an instance variable exist?", "One per object created", "Exactly one for the whole program", "None until the program ends"],
  "constructor-overload": ["Constructor Overloading", "Several constructors with different parameter lists so objects can be created in flexible ways.", "Ordering coffee: default, with size, or fully customised.", "S.N.D.P.T = Same Name, Different Params, this() chains", "public class Book {\n  private String title; private int pages;\n  public Book() { this(\"Untitled\", 0); }\n  public Book(String title) { this(title, 0); }\n  public Book(String title, int pages) { this.title = title; this.pages = pages; }\n}", "What does this(...) do inside a constructor?", "Calls another constructor in the same class", "Creates a second object", "Returns the object"],
  "this-keyword": ["this Keyword", "A reference to the current object, used to separate fields from parameters and to chain constructors.", "Saying \"my own\" when two people share the same name.", "F.P.C = Field vs Parameter, Constructor chain", "public class User {\n  private String name;\n  public User(String name) { this.name = name; }\n  public User rename(String name) { this.name = name; return this; }\n}", "Why write this.name = name?", "To assign the parameter to the object's own field", "To create a new variable", "To delete the field"],
  "access-modifiers": ["Access Modifiers", "Keywords controlling who can see a field or method: public, protected, default, private.", "House rules: front garden (public), family rooms (protected), your diary (private).", "P.P.D.P = Public, Protected, Default, Private", "public class Account {\n  private double balance;       // only this class\n  protected String owner;       // subclasses + package\n  String branch;                // package only\n  public double getBalance() { return balance; }\n}", "Which modifier gives the tightest access?", "private — visible only inside the same class", "public", "protected"],
  "getters-setters": ["Getters & Setters", "Public methods that read and safely update private fields, letting you validate changes.", "A bank teller: you never reach into the vault yourself.", "G.S.V = Get returns, Set validates", "private int age;\npublic int getAge() { return age; }\npublic void setAge(int age) {\n  if (age >= 0) this.age = age;\n}", "What is the point of a setter over a public field?", "It lets you validate or react to changes before storing them", "It runs faster", "It makes the field public"],
  "static-members": ["Static Members", "Fields and methods that belong to the class itself, shared by every object.", "The shop's opening hours sign — one for the whole shop, not one per customer.", "C.S.N = Class-level, Shared, No object needed", "public class Counter {\n  static int total = 0;\n  Counter() { total++; }\n  static int getTotal() { return total; }\n}\nCounter.getTotal();", "How do you call a static method?", "Through the class name, without creating an object", "Only through an object", "Only inside a constructor"],
  "super-keyword": ["super Keyword", "Refers to the parent class — used to call its constructor or its version of a method.", "Asking your parent how they used to do it before adding your own twist.", "S.C.M = Super Constructor, super.Method", "class Animal { void speak() { System.out.println(\"...\"); } }\nclass Dog extends Animal {\n  Dog() { super(); }\n  void speak() { super.speak(); System.out.println(\"Woof\"); }\n}", "What does super() do in a constructor?", "Calls the parent class constructor first", "Creates a parent object separately", "Skips the parent class"],
  "method-overriding": ["Method Overriding", "A subclass replaces the parent's version of a method with its own, using the same signature.", "A cover version of a song: same title, different performance.", "S.S.A = Same Signature, @Override Annotation", "class Shape { double area() { return 0; } }\nclass Circle extends Shape {\n  double r = 2;\n  @Override double area() { return Math.PI * r * r; }\n}", "What must match when overriding?", "The method name, parameters and compatible return type", "Only the method name", "Only the return type"],
  "object-class": ["Object Class", "Every Java class silently extends Object, inheriting toString, equals and hashCode which you usually override.", "A default passport everyone is born with — you can personalise the photo.", "T.E.H = toString, equals, hashCode", "@Override public String toString() { return name + \" (\" + age + \")\"; }\n@Override public boolean equals(Object o) {\n  if (!(o instanceof User u)) return false;\n  return name.equals(u.name);\n}", "Why override toString()?", "So printing the object shows meaningful text instead of a memory reference", "To make the class run faster", "It is required by the compiler"],
  "dynamic-binding": ["Dynamic Binding", "Java decides at run time which overridden method to run, based on the real object type.", "Calling \"play\" on a band: whichever musician holds the instrument decides the sound.", "R.T.O = Run Time, actual Object wins", "Shape s = new Circle();\nSystem.out.println(s.area()); // Circle's area runs", "Which method runs when a Shape variable holds a Circle?", "Circle's overridden method, decided at run time", "Shape's method, decided at compile time", "Neither — it is an error"],
  "instanceof-op": ["instanceof Operator", "Checks whether an object is of a given type before casting, avoiding ClassCastException.", "Checking the label on a box before opening it with the wrong tool.", "C.B.C = Check Before Cast", "if (animal instanceof Dog d) {\n  d.fetch();\n}", "Why check instanceof before casting?", "To avoid a ClassCastException at run time", "To speed up the cast", "Because casting is illegal otherwise"],
  "abstract-classes": ["Abstract Classes", "A partly finished blueprint that cannot be instantiated but supplies shared code to subclasses.", "A house plan with the walls drawn but rooms left for the buyer to finish.", "N.I.S = No Instances, Shared code", "abstract class Vehicle {\n  int wheels;\n  void start() { System.out.println(\"Starting\"); }\n  abstract double fuelCost();\n}", "Can you write new Vehicle()?", "No — abstract classes cannot be instantiated directly", "Yes, always", "Only inside main"],
  "abstract-methods": ["Abstract Methods", "A method signature with no body that every concrete subclass must implement.", "A job description with a task listed but no instructions — each employee decides how.", "N.B.M.I = No Body, Must Implement", "abstract class Shape {\n  abstract double area();\n}\nclass Square extends Shape {\n  double s = 3;\n  double area() { return s * s; }\n}", "What must a concrete subclass do with an abstract method?", "Provide an implementation for it", "Ignore it", "Delete it from the parent"],
  "interface-default": ["Default Methods", "Interfaces can supply a ready-made method body with default, so adding methods does not break existing code.", "A new appliance feature that ships pre-configured but can be changed.", "D.B.O = Default Body, Overridable", "interface Greeter {\n  String name();\n  default String greet() { return \"Hello, \" + name(); }\n}", "Why were default methods added to Java?", "To add methods to interfaces without breaking existing implementations", "To make interfaces run faster", "To replace abstract classes entirely"],
  "multiple-interfaces": ["Multiple Interfaces", "A class can implement many interfaces, gaining several capabilities even though it extends only one class.", "One person can be a driver, a cook and a swimmer at once.", "O.P.M.C = One Parent, Many Contracts", "class Robot implements Movable, Chargeable, Comparable<Robot> {\n  public void move() {}\n  public void charge() {}\n  public int compareTo(Robot o) { return 0; }\n}", "How many classes can a Java class extend?", "Exactly one, but it can implement many interfaces", "As many as you like", "None"],
  "try-catch": ["Try-Catch Blocks", "Run risky code in try; if it fails, catch handles the error instead of crashing the program.", "A safety net under a trapeze artist.", "T.R.C.H = Try Risky Code, Handle it", "try {\n  int result = 10 / divisor;\n  System.out.println(result);\n} catch (ArithmeticException e) {\n  System.out.println(\"Cannot divide by zero\");\n}", "What happens to the program when an exception is caught?", "It keeps running using the catch block's handling", "It always terminates", "It restarts"],
  "multiple-catch": ["Multiple Catch Blocks", "Several catch blocks handle different exception types, from most specific to most general.", "Sorting post into labelled pigeonholes, with a spare 'other' tray at the end.", "S.F.G.L = Specific First, General Last", "try {\n  process(input);\n} catch (NumberFormatException e) {\n  System.out.println(\"Not a number\");\n} catch (IOException | SQLException e) {\n  System.out.println(\"Resource problem\");\n} catch (Exception e) {\n  System.out.println(\"Unexpected\");\n}", "Why must specific exceptions come first?", "A general catch first would swallow every exception and make later blocks unreachable", "Order does not matter", "The compiler sorts them for you"],
  "finally-block": ["Finally Block", "Code that always runs after try/catch, used to release resources whatever happens.", "Locking the shop door whether or not you made a sale.", "A.R.C = Always Runs, Cleanup", "try {\n  file = open(path);\n  read(file);\n} catch (IOException e) {\n  log(e);\n} finally {\n  close(file); // always runs\n}", "When does finally run?", "After try/catch, whether or not an exception happened", "Only when there is no exception", "Only when there is an exception"],
  "throw-throws": ["Throw & Throws", "throw raises an exception now; throws declares that a method may pass one to its caller.", "throw is pulling the alarm; throws is the warning sign on the door.", "T.N.D = Throw Now, Declare with throws", "public void withdraw(double amt) throws InsufficientFundsException {\n  if (amt > balance) throw new InsufficientFundsException(\"Too much\");\n  balance -= amt;\n}", "What does throws in a method signature mean?", "This method may pass that exception to its caller", "The method always fails", "It catches the exception"],
  "custom-exceptions": ["Custom Exceptions", "Your own exception classes give errors meaningful names specific to your domain.", "A labelled warning light for 'low coolant' instead of a generic 'fault'.", "E.N.M = Extend Exception, Name it, pass Message", "public class InvalidAgeException extends Exception {\n  public InvalidAgeException(String msg) { super(msg); }\n}\nthrow new InvalidAgeException(\"Age must be positive\");", "Why create a custom exception?", "It communicates exactly what went wrong in your domain", "It runs faster", "It stops all errors"],
  "list-interface": ["List Interface", "An ordered collection allowing duplicates — ArrayList for fast reads, LinkedList for fast inserts.", "A numbered queue where the same name can appear twice.", "O.D.I = Ordered, Duplicates, Indexed", "List<String> a = new ArrayList<>();\nList<String> l = new LinkedList<>();\na.add(\"x\"); a.add(\"x\");\nSystem.out.println(a.get(1));", "When is LinkedList a better choice than ArrayList?", "When you insert or remove frequently at the ends or middle", "When you need fast random access", "Always"],
  "set-interface": ["Set Interface", "A collection with no duplicates — HashSet is unordered and fast, TreeSet keeps items sorted.", "A bag of unique stamps: adding the same one twice changes nothing.", "U.H.T = Unique, Hash fast, Tree sorted", "Set<String> tags = new HashSet<>();\ntags.add(\"java\"); tags.add(\"java\");\nSystem.out.println(tags.size()); // 1\nSet<String> sorted = new TreeSet<>(tags);", "What happens when you add a duplicate to a Set?", "Nothing changes — the set already contains it", "It is added twice", "An exception is thrown"],
  "map-interface": ["Map Interface", "Stores key to value pairs for instant lookup by key — HashMap unordered, TreeMap sorted.", "A dictionary: look up the word, get the meaning.", "K.V.P.G = Key, Value, put, get", "Map<String, Integer> scores = new HashMap<>();\nscores.put(\"Ana\", 90);\nscores.put(\"Bo\", 75);\nSystem.out.println(scores.get(\"Ana\"));\nfor (var e : scores.entrySet()) System.out.println(e.getKey() + \"=\" + e.getValue());", "What happens if you put the same key twice?", "The new value replaces the old one", "Both values are stored", "It throws an exception"],
  "iterators": ["Iterators", "An object that walks a collection one item at a time and can safely remove during traversal.", "A conveyor belt handing you items one by one.", "H.N.R = hasNext, next, remove", "Iterator<String> it = list.iterator();\nwhile (it.hasNext()) {\n  String s = it.next();\n  if (s.isEmpty()) it.remove();\n}", "Why use an Iterator to remove items?", "Removing inside a for-each loop throws ConcurrentModificationException", "It is shorter to type", "It sorts the list"],
  "generic-classes": ["Generic Classes", "Classes parameterised by a type so one class works safely with any type you choose.", "A labelled storage box: the label tells everyone exactly what is inside.", "T.S.R = Type parameter, Safety, Reuse", "public class Box<T> {\n  private T item;\n  public void set(T item) { this.item = item; }\n  public T get() { return item; }\n}\nBox<String> b = new Box<>();", "What is the main benefit of generics?", "Compile-time type safety with no casting", "Faster execution", "Less memory used"],
  "generic-methods": ["Generic Methods", "A single method that works with any type, declaring its own type parameter before the return type.", "One universal adapter plug that fits every socket.", "A.T.R = Angle brackets before Return type", "public static <T> void printAll(List<T> items) {\n  for (T item : items) System.out.println(item);\n}\npublic static <T extends Comparable<T>> T max(T a, T b) {\n  return a.compareTo(b) >= 0 ? a : b;\n}", "Where does <T> go in a generic method?", "Before the return type in the method signature", "After the method name", "Inside the parameter list"],
  "wildcards": ["Wildcards", "? extends T reads from a producer; ? super T writes to a consumer.", "A postbox: extends is a window you look through, super is a slot you post into.", "P.E.C.S = Producer Extends, Consumer Super", "double total(List<? extends Number> nums) {\n  double t = 0;\n  for (Number n : nums) t += n.doubleValue();\n  return t;\n}\nvoid fill(List<? super Integer> dst) { dst.add(1); }", "What does PECS stand for?", "Producer Extends, Consumer Super", "Public Extends, Class Super", "Parameter Even, Class Same"],
  "functional-interfaces": ["Functional Interfaces", "Interfaces with exactly one abstract method, letting you pass behaviour as a lambda.", "Handing someone a written instruction instead of doing the job yourself.", "P.C.F.S = Predicate, Consumer, Function, Supplier", "Predicate<String> isLong = s -> s.length() > 5;\nConsumer<String> print = s -> System.out.println(s);\nFunction<Integer, Integer> sq = n -> n * n;\nSupplier<Double> rnd = Math::random;", "What defines a functional interface?", "It has exactly one abstract method", "It has no methods", "It must be public"],
  "streams-intro": ["Streams Introduction", "A pipeline that processes collections declaratively: describe what you want, not how to loop.", "A factory conveyor: items flow through filters and shapers to a finished box.", "S.I.T = Source, Intermediate ops, Terminal op", "List<String> result = names.stream()\n  .filter(n -> n.startsWith(\"A\"))\n  .map(String::toUpperCase)\n  .toList();", "What triggers a stream to actually run?", "The terminal operation, such as toList or forEach", "Creating the stream", "The filter call"],
  "stream-operations": ["Stream Operations", "filter selects, map transforms, sorted orders, reduce combines into a single result.", "Sorting laundry: pick out whites, wash them, fold them, count them.", "F.M.S.R = filter, map, sorted, reduce", "int total = nums.stream()\n  .filter(n -> n % 2 == 0)\n  .map(n -> n * 2)\n  .reduce(0, Integer::sum);", "What does map do in a stream?", "Transforms each element into another value", "Removes elements", "Sorts the elements"],
  "collectors": ["Collectors", "Terminal helpers that gather stream results into lists, sets, maps, joined text or grouped buckets.", "Emptying a conveyor into the right labelled crates at the end of the line.", "T.T.J.G = toList, toSet, joining, groupingBy", "Map<String, List<User>> byCity = users.stream()\n  .collect(Collectors.groupingBy(User::getCity));\nString csv = names.stream().collect(Collectors.joining(\", \"));", "What does Collectors.groupingBy produce?", "A Map of keys to lists of matching elements", "A sorted list", "A single joined String"],
};
